# p5.utils
 A set of utilities and additional features for my creative coding class aiming to help students while introducing the algorithmic thinking.  

## The list of utilities in p5.Utils repository;

[Pixel Ruler](https://github.com/alptugan/p5.utils/tree/main/examples/1_pixelRuler) | [DOM Debug Window](https://github.com/alptugan/p5.utils/tree/main/examples/2_debugView) | [Get time-stamp](https://#)
------------- | ------------- | -------------
<img src="examples/1_pixelRuler/ss-ruler.jpg" width="660">  | <img src="examples/2_debugView/ss-debug.jpg" width="600"> | ![]()


## How to use p5.Utils library?
1. Download the final minified js version from ["Releases" page](https://github.com/alptugan/p5.utils/releases/download/v0.0.1/p5.utils.min.js).
2. Upload ```p5.utils.min.js``` to your project folder in p5 editor.
3. Include the ```p5.utils.min.js``` in the ```index.html``` document before p5.js libs as follows;

```
    <script src="libraries/p5.min.js"></script>
    <script src="libraries/p5.sound.min.js"></script>
    <script src="libraries/p5.utils.min.js"></script>
```
4. Declare and initialize the lib before the ```setup()``` prefably as follows;

```
var utils = new p5.Utils();
```
5. Now you can call any method defined in ```p5.utils```library using dot notation as follows in ```setup()``` or any other custom methods in your code;

```
utils.enablerRuler(); 
```

## Reference
### p5.Utils library
p5.Utils extends p5 with several functionalities including cheaper drawingcontext effects, pixel ruler (useful for new commers), array operations, file naming, dom based debug window to avoid rendering text in p5JS.

<!-- Generated by documentation.js. Update this documentation by updating the source code. -->

### Table of Contents

*   [p5.Utils][1]
*   [debug][2]
    *   [Parameters][3]
    *   [Examples][4]
*   [getTimeStamp][5]
    *   [Parameters][6]
    *   [Examples][7]
*   [saveCanvas][8]
    *   [Parameters][9]
    *   [Examples][10]
*   [arrayResize][11]
    *   [Parameters][12]
    *   [Examples][13]
*   [beginShadow][14]
    *   [Parameters][15]
    *   [Examples][16]
*   [endShadow][17]
*   [beginLinearGradient][18]
    *   [Parameters][19]
    *   [Examples][20]
*   [endLinearGradient][21]
*   [disableRuler][22]
*   [enableRuler][23]
    *   [Examples][24]

## p5.Utils

*   **See**: [About the author][25]

Base class for the library

**Meta**

*   **version**: 0.0.1
*   **author**: Alp Tuğan

## debug

Create Debug Div cause p5 font is expensive.

### Parameters

*   `_itemName` **([Object][26]<[string][27], [number][28]> | [Object][26]<[string][27], [string][27]>)** The argument must be in JSON data format. The function automatically parses "keys" to titles and their "values" next to them. You can add as many objects as you want.

### Examples

How to use debug() method.

```javascript
// Define global variable and initialize p5.Utils lib
var utils = new p5.Utils();

utils.debug(
{
   "FPS": frameRate().toFixed(0),
   "Frequency": frequency.toFixed(3)
});
```

## getTimeStamp

Timestamp function useful for file naming to avoid overwrite issues.

### Parameters

*   `_date` **[boolean][29]** If true -> Timestamp within Year-Month-Day (optional, default `true`)

### Examples

```javascript
// Define global variable and initialize p5.Utils lib
var utils = new p5.Utils();
var currentTime = utils.getTimeStamp();
```

Returns **[String][27]** Current date + time depending on \_date argument value.
When \_date = true;
The return format is Year-Month-Day_Hour-Minute-Second
When \_date = false;
The return format is Hour-Minute-Second

## saveCanvas

Utilizes p5JS saveCanvas function to make it easier file saving process by combining the function with getTimeStamp() method.

### Parameters

*   `_prefix` **([String][27] | [Number][28])** Any relevant text in the begining of the file name. If it is leaved empty, the file name will be Year-Month-Day_Hour-Minute-Second.PNG (optional, default `""`)
*   `_suffix` **[String][27]** The file extension JPG, PNG, ... (optional, default `"png"`)

### Examples

```javascript
// Define global variable and initialize p5.Utils lib
var utils = new p5.Utils();
function keyPressed() {
     if(key == 's') {
         utils.saveCanvas("myProject", "png");
         // or without any argument
         // utils.saveCanvas();
     }
}
```

## arrayResize

### Parameters

*   `_arr` **([Array][30]<[Number][28]> | [Array][30]<[String][27]> | [Array][30]<[Boolean][29]>)** The array to be resized
*   `_newSize` **[Number][28]** The new size of the array
*   `_defaultValue` **([Number][28] | [String][27] | [Boolean][29])** Default value for all members of the array. By default it's 0. (optional, default `0`)

### Examples

```javascript
// Define global variable and initialize p5.Utils lib
var utils = new p5.Utils();
var arr = [];
arr = utils.arrayResize(arr,10);
print(arr);

// or assign default values
arr = utils.arrayResize(arr, 22, random(0,1));
print(arr);
```

Returns **([Array][30]<[Number][28]> | [Array][30]<[String][27]> | [Array][30]<[Boolean][29]>)** The new array

## beginShadow

Creates shadow effect usign drawing context. Must be used with [endShadow][17] method. See examples for how to use it.

### Parameters

*   `_color` **(p5.Color | [String][27])** The color can be declared as `color(r,g,b,a)` or in hexadecimal format `"#FFCC00"` as string argument.
*   `_shadowBlur` **[Number][28]** Blur amount of the shadow.
*   `_shadowOffsetX` **[Number][28]** Shadow offset for x axis.
*   `_shadowOffsetY` **[Number][28]** Shadow offset for y axis.

### Examples

```javascript
var utils = new p5.Utils();
function setup() {
     createCanvas(400,400);
     rectMode(CENTER);
}

function draw() {
     utils.beginShadow("#000000", 5, 10, 10);
     rect(width*0.5, height*0.5, 100, 100);
     utils.endShadow();
}
```

## endShadow

*   **See**: For example usage [beginShadow][14] page.

Stops shadow effect for the following graphics on the canvas.

## beginLinearGradient

Default Context 2D Gradient fill style.

Reference: <a>[https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D/createLinearGradient][31]</a>

### Parameters

*   `_colorsArr` **([Array][30]\<p5.Color> | [Array][30]<[Number][28]> | [Array][30]<[String][27]>)** List of colors in the gradient fill.
*   `_startX` **[Number][28]** The x-axis coordinate of the start point.
*   `_startY` **[Number][28]** The y-axis coordinate of the start point.
*   `_endX` **[Number][28]** The x-axis coordinate of the end point.
*   `_endY` **[Number][28]** The y-axis coordinate of the end point.
*   `_colorsRatio` **[Array][30]<[Number][28]>** The distribution weight of colors. The values must be between 0 - 1. Conventionally, if you include three colors, set the first one to 0, the last one to 1, and the middle one depends on your choice(0-1). The method automatically assign start and stop values, if you do not specify any value they will be generated randomly.

### Examples

```javascript
// Define global variable and initialize p5.Utils lib
var utils = new p5.Utils();

function setup() {
     createCanvas(400,400);
}

function draw() {
     utils.beginLinearGradient(
     [color(100,100,0),color(100,130,200)], 
     width*0.5 - 100, 
     height*0.5 - 100, 
     width*0.5+100, 
     height*0.5+100);
     circle(width*0.5, height*0.5,500);
     utils.endLinearGradient();
}
```

## endLinearGradient

*   **See**: For example usage [beginLinearGradient][18] page.

Stop Gradient fill for the following graphics.

## disableRuler

*   **See**: For example usage [enableRuler][23] page.

Removes the ruler graphics from the canvas.

## enableRuler

Ruler for newcomers to show pixel meter

### Examples

```javascript
* // Define global variable and initialize p5.Utils lib
var utils = new p5.Utils();

function setup() {
     createCanvas(400,400);
     
     // No need to run in draw function
     // The function creates its canvases in a different drawing context
     utils.enableRuler();
}

function draw() {
     background(220);
     rect(width*0.5, height*0.5,500, 500);
}

function keyPressed() {
     if(key == 'h') {
         utils.disableRuler();
     }
}
```

[1]: #p5utils

[2]: #debug

[3]: #parameters

[4]: #examples

[5]: #gettimestamp

[6]: #parameters-1

[7]: #examples-1

[8]: #savecanvas

[9]: #parameters-2

[10]: #examples-2

[11]: #arrayresize

[12]: #parameters-3

[13]: #examples-3

[14]: #beginshadow

[15]: #parameters-4

[16]: #examples-4

[17]: #endshadow

[18]: #beginlineargradient

[19]: #parameters-5

[20]: #examples-5

[21]: #endlineargradient

[22]: #disableruler

[23]: #enableruler

[24]: #examples-6

[25]: https://www.alptugan.com

[26]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Object

[27]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/String

[28]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Number

[29]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Boolean

[30]: https://developer.mozilla.org/docs/Web/JavaScript/Reference/Global_Objects/Array

[31]: https://developer.mozilla.org/en-US/docs/Web/API/CanvasRenderingContext2D/createLinearGradient

